enum ConnectionEvents {
    ALLOW_CONNECTION = "Rebirth:connection:server:allowConnection",
    START_LOGIN = "Rebirth:connection:server:startLogin",
    LOGIN_DISCONNECT = "Rebirth:connection:server:disconnect",
    LOGIN_CHARACTER_SELECT = "Rebirth:connection:server:characterSelect",
}

export default ConnectionEvents;